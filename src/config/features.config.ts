/**
 * Configuración de Features - Active/Desactive funcionalidades aquí
 */

export const FEATURES = {
  /**
   * FILTRO DE PROXIMIDAD
   * true = Solo muestra libros dentro del radio del usuario
   * false = Muestra TODOS los libros (ignora ubicación)
   * 
   * 🔧 Para desactivar temporalmente: cambiar a false
   */
  PROXIMITY_FILTER_ENABLED: true,
};
